version: '3.0'

volumes:
  script-neo4j:

services:
  neo4j:
    image: neo4j:latest
    container_name: script-neo4j
    ports:
      - '7474:7475'
      - '7687:7688'

    env_file:
      - path: .env
        required: true

    volumes:
      - script-neo4j:/data


